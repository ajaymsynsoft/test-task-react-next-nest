import { Style } from '@/types'

export const style: Style = {
  box: {
    maxWidth: 750,
    gap: 4,
    borderRadius: 2.5,
    bgcolor: 'background.default',
    mx: 'auto',
  },
  socialLoginContainer: {
    alignItems: 'center',
    gap: 2,
  },
  submitBtn: {
    width: 1,
    mx: 'auto',
    maxWidth: 500,
  },
}
